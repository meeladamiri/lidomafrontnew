# وضعیت پروژه — مهاجرت لیدوماتریپ از Odoo به بک‌اند جدید

آخرین به‌روزرسانی: 2026-08-25 (نشست ششم — درخت مکان‌ها و سیستم تگ سئو)

> این فایل «نقشه‌ی برگشت» پروژه‌ست. اگه یه چت جدید (یا ادامه‌ی همین چت بعد از خلاصه‌سازی خودکار) این پروژه رو باز کرد، خوندن همین یه فایل باید کافی باشه بفهمه دقیقاً کجای کاریم. کد واقعی همیشه توی گیت‌هابه، نه توی تاریخچه‌ی چت — پس اگه مکالمه از دست بره چیزی از دست نمی‌ره.
>
> این فایل همیشه **آخرین وضعیت** رو نشون می‌ده (هر بار بازنویسی می‌شه). یه کپی تاریخ‌دار از هر نسخه هم توی `docs/progress/YYYY-MM-DD_HH-MM.md` آرشیو می‌شه.

---

## این پروژه دقیقاً چیه؟

**لیدوماتریپ قبلاً روی Odoo اجرا می‌شد** — بک‌اند production واقعی (`lidomatrip.com`) هنوز روی Odoo زنده‌ست. هدف: مهاجرت کامل به یه بک‌اند جدید و مستقل (Express + TypeScript + Prisma + PostgreSQL)، بدون دست‌زدن به کامپوننت‌های UI فرانت‌اند قدیمی (فقط لایه‌ی `api/*.ts` عوض می‌شه).

**مرحله‌ی «کارکردی»** (فاز ۱) و **مرحله‌ی «دیتای واقعی»** هر دو کامل شدن. نشست پنجم **ریدیزاین پنل ادمین** بود و نشست ششم **درخت مکان‌ها + سیستم تگ سئو**.

### حساب‌های تست

| نقش | شماره | رمز |
|---|---|---|
| ادمین | `09120000000` | `123456` |
| میزبان | `09120000001` | `123456` |
| مهمان | `09120000002` | `123456` |

پنل ادمین: `/admin/login` · سایت: `/auth/enter_phone` (ورود با رمز)

---

## 🟢 وضعیت دیتا (آمار واقعی از دیتابیس)

| بخش | تعداد |
|---|---|
| کاربران | ۲۸۶,۵۲۷ |
| اقامتگاه‌ها | ۹,۵۷۳ (سوئیت ۸,۳۱۹ · بوم‌گردی ۱,۲۴۹ · هتل ۵) |
| اقامتگاه با شهر مشخص | ۹,۳۹۴ |
| رزروها | ۲۹,۶۴۹ |
| نظرات | ۹,۴۲۷ |
| کاتالوگ امکانات / ویژگی‌هاشون | ۴۲ / ۱۱۵ |
| کاتالوگ قوانین | ۸ |
| لینک امکانات ↔ اقامتگاه | ۱۵۰,۰۶۶ |
| لینک قوانین ↔ اقامتگاه | ۲۸,۵۴۷ |
| فاصله تا جاذبه‌ها | ۱۴,۸۶۶ |
| شهرهای فرعی اقامتگاه | ۸۵۹ |
| اهمیت اقامتگاه (غیرصفر) | ۷,۵۳۸ |
| ریدایرکت‌های سئویی `/tags/…` | ۹,۸۹۰ |
| ریدایرکت‌های عکس `/web/image/…` | ۱۰۷,۷۴۳ |
| مکان‌ها (کشور/استان/شهر/منطقه/روستا/محله) | ۵۶۵ |
| ست‌های سئوی مکان (`location_seo`) | ۱٬۶۷۵ |
| شهرهای زیرمجموعه (`location_includes`) | ۱۹ |
| تگ‌های سئو / شرط‌هاشون | ۱۸ / ۲۲ |
| صفحات تگ×مکان (`tag_pages`) | ۹٬۳۱۲ |

**مهاجرت دیتا ۱۰۰٪ کامله.** همه‌ی اسکریپت‌ها idempotent هستن؛ اگه وسط راه با خطای connection موقت (Liara زیر بار سنگین) قطع شدن، فقط دوباره همون دستور رو بزن.

### ترتیب اجرای اسکریپت‌ها (توی `backend/`)

```bash
npx tsx --env-file=.env scripts/migrate-odoo-users.ts --commit
npx tsx --env-file=.env scripts/migrate-odoo-residences.ts --commit    # نیازمند image-map.json از سرور مبدأ
npx tsx --env-file=.env scripts/migrate-odoo-cities.ts --commit
npx tsx --env-file=.env scripts/fix-city-slugs-from-odoo.ts --commit   # اسلاگ واقعی x_title_en
npx tsx --env-file=.env scripts/migrate-odoo-city-seo.ts --commit      # متا و متن راهنمای شهرها
npx tsx --env-file=.env scripts/migrate-odoo-reservations.ts --commit
npx tsx --env-file=.env scripts/migrate-odoo-reviews.ts --commit       # ۹,۴۲۶ نظر
npx tsx --env-file=.env scripts/migrate-odoo-amenities.ts --commit     # کاتالوگ + لینک‌ها
npx tsx --env-file=.env scripts/migrate-odoo-amenity-details.ts --commit  # ساب‌فیچر، آیکون، قوانین متنی
npx tsx --env-file=.env scripts/migrate-odoo-residence-extras.ts --commit # فاصله‌ها، شهرهای فرعی، آدرس فاکتور
npx tsx --env-file=.env scripts/backfill-residence-type.ts --commit    # نوع ملک واقعی
npx tsx --env-file=.env scripts/backfill-residence-importance.ts --commit
npx tsx --env-file=.env scripts/backfill-odoo-latlng.ts --commit
npx tsx --env-file=.env scripts/migrate-odoo-tag-urls.ts --commit
npx tsx --env-file=.env scripts/migrate-odoo-image-urls.ts --commit
npx tsx --env-file=.env scripts/migrate-odoo-calendar.ts --commit
npx tsx --env-file=.env scripts/backfill-host-avatars.ts --commit      # نیازمند host-avatar-map.json

# --- نشست ششم: تاکسونومی (بعد از prisma migrate deploy اجرا شن) ---
npx tsx --env-file=.env scripts/migrate-odoo-locations.ts --commit   # درخت مکان + ۳ ست سئو + زیرمجموعه‌ها
npx tsx --env-file=.env scripts/migrate-odoo-seo-tags.ts --commit    # ۱۸ تگ از روی x_domain
npx tsx --env-file=.env scripts/migrate-odoo-tag-pages.ts --commit   # ۹٬۳۱۲ صفحه‌ی تگ×مکان

# گیت‌های اجباری بعد از هر تغییر در سرچ یا مکان‌ها
npx tsx --env-file=.env scripts/verify-location-slugs.ts --compare <snapshot.json>
npx tsx --env-file=.env scripts/verify-search-parity.ts <snapshot.json>
```

**بازنشسته:** `backfill-city-slugs.ts` (ترنسلیتره‌هاش غلط بودن، یزد→izd؛ جایگزینش `fix-city-slugs-from-odoo.ts`).

---

## 🎨 پنل ادمین — وضعیت فعلی

با **Tailwind** و توکن‌های خود پروژه (`text-14`, `rounded-12`, `gray-*`, `primary-main`) بازنویسی می‌شه. **آیکون‌ها از فونت آیکون خود پروژه** (`styles/icomoon.css`، ۱۸۴ آیکون، از قبل گلوبال) — هیچ کتابخانه‌ی آیکونی نصب نشد.

### فایل‌های کلیدی

| فایل | نقش |
|---|---|
| `front/components/Admin/ui.tsx` | همه‌ی پریمیتیوها: Card, Badge, Button, Input/Select/Field, StatTile, Modal, Toggle, Skeleton, **Toolbar/ToolbarSearch/ToolbarPager/ViewSwitch/TabPills**, **SelectionBar/RowMenu/Checkbox/Stars**, `adminImageUrl()`, فرمترهای فارسی |
| `front/components/Admin/Layout.tsx` | سایدبار آیکونی بازشونده با هاور + هدر مشترک (پروفایل، اعلان، تمام‌صفحه، جستجو، breadcrumb) + اسلات `toolbar` |
| `front/components/Admin/LocationPicker.tsx` | نقشه‌ی Leaflet برای انتخاب lat/lng (کلیک یا کشیدن پین) |
| `front/components/Admin/Residence/*.tsx` | تب‌های صفحه‌ی اقامتگاه: `CapacityTab`, `AmenitiesTab`, `PricingTab`, `RulesTab` |
| `front/pages/admin/settings/index.tsx` | تنظیمات سراسری: امکانات، قوانین، روزهای پیک |
| `front/pages/admin/settings/locations.tsx` | درخت مکان‌ها: فیلتر نوع، جدول، مودال جزئیات، زیرمجموعه‌ها، ۳ ست سئو |
| `front/pages/admin/settings/tags.tsx` | تگ‌های سئو: سازنده‌ی شرط (و/یا) + پیش‌نمایش زنده‌ی تعداد نتایج |
| `backend/src/lib/location.ts` | resolve اسلاگ و expand نتایج — **قلب حفظ آدرس‌های ایندکس‌شده** |
| `backend/src/lib/seoTags.ts` | تعریف تگ‌ها از دیتابیس + تبدیلشون به فیلتر Prisma |
| `backend/src/modules/admin/taxonomy.service.ts` | CRUD مکان/تگ/صفحه‌ی تگ با گاردهای سئویی |

### صفحات ساخته‌شده

- **داشبورد** — ۴ کارت گرادیانی با درصد تغییر ماهانه، نمودار ۱۲ ماهه‌ی رزرو (بدون کتابخانه‌ی چارت)، کارت‌های کار در انتظار، آخرین کاربران/رزروها
- **کاربران** — تب‌های شمارنده‌دار، جستجو/فیلتر/مرتب‌سازی، نمای لیست و کارتی، توگل فعال‌سازی، مودال ایجاد کاربر
- **جزئیات کاربر** — هدر گرادیانی، ۴ کارت آماری، ویرایش، تنظیم رمز، **کارت زرد**، اقامتگاه‌های کاربر
- **اقامتگاه‌ها** — تب‌های نوع ملک، نمای لیست (با منوی ⋮) و کارتی، **نوار انتخاب گروهی** (خروجی CSV، حذف، کپی، غیرفعال‌سازی، تغییر نوع ملک)
- **جزئیات اقامتگاه** — ۱۰ تب (پایین‌تر)
- **تنظیمات** — امکانات (+ تعریف ساب‌فیچرها)، قوانین، روزهای پیک
- **دسته‌بندی مکان‌ها** — شمارنده‌ی نوع‌ها، جستجو، هشدار اسلاگ تکراری، مودال جزئیات با بردکرامب و canonical زنده، انتخابگر «شهرهای زیرمجموعه»، ۳ ست سئو با هشدار طول متا
- **تگ‌های سئو** — لیست تگ‌ها، سازنده‌ی شرط گروهی (داخل گروه «یا»، بین گروه‌ها «و»)، پیش‌نمایش زنده‌ی تعداد اقامتگاه، هشدار «این تگ فیلتری نداره»

### تب‌های صفحه‌ی جزئیات اقامتگاه

| تب | وضعیت |
|---|---|
| اطلاعات پایه | ✅ ریل اکشن، گالری، نوار وضعیت/میزبان، مشخصات، آدرس + **نقشه‌ی تعاملی** + فاصله تا جاذبه‌ها، دو مودال ویرایش |
| ظرفیت | ✅ ظرفیت پایه/حداکثر، فضای مشترک + اتاق‌ها با تخت‌ها و «سایر موارد» |
| امکانات | ✅ گرید چک‌باکسی گروه‌بندی‌شده با آیکون اختصاصی + مودال «ویژگی ها» |
| قوانین و مقررات | ✅ توگل قوانین، حداقل شب/نفر، ساعت ورود/خروج، ۳ سیاست لغو |
| نرخ اقامتگاه | ✅ شامل **نرخ نفر اضافه ایام پیک** (فیلد جدید) |
| رزروها · تقویم · آمار · نظرات · مدرک مالکیت | ⬜ هنوز نه (placeholder) |

---

## تصمیمات کلیدی (چراها)

1. **id عمومی اقامتگاه = id اودو** (`backend/src/lib/publicId.ts`). URLهای `/rentals/<id>` توی گوگل ایندکس شدن؛ اقامتگاه‌های migrate‌شده با همون id آدرس‌دهی می‌شن، id داخلی‌شون ۴۰۴ می‌ده (جلوگیری از duplicate content). `residences_id_seq` به ۵۰,۰۰۰ جهش داده شد تا رکورد جدید با بازه‌ی اودو تداخل نکنه.
2. **«نوع ملک» سه‌مقداریه** (`SUIT`/`BOOMGARDI`/`HOTEL`) و از `x_display_type` میاد. نگاشت slug/لیبل توی **یه فایل** (`backend/src/lib/residenceType.ts` + `front/utilities/residenceType.ts`) — نه ternary پراکنده.
3. **کلید پایدار امکانات/قوانین = `key`** (همون `x_title_en` اودو) — فیلترهای جستجو روی همین مچ می‌کنن و اتفاقاً با ثابت‌های UI فرانت یکی بودن.
4. **ساب‌فیچرها دیتا هستن نه کد** — تعریفشون توی `AmenityFeature` (از `x_extra_info`)، جواب‌هاشون توی `ResidenceAmenity.extraFeatures.extra`. مودال «ویژگی ها» از روی همین رندر می‌شه، پس افزودن ویژگی جدید فقط تنظیمات می‌خواد.
5. **متن‌های آزاد بدون HTML ذخیره می‌شن** و با `whitespace-pre-line` رندر می‌شن — نه `<br>` تزریقی و نه `dangerouslySetInnerHTML` (امن‌تر در برابر متن میزبان).
6. **اسکریپت‌ها همیشه dry-run پیش‌فرض** (`--commit` برای نوشتن) و idempotent.
7. **اکشن‌های گروهی حذف نرم دارن** (`state=DELETED`) تا سابقه‌ی رزروها حفظ شه.
8. **روزهای پیک سراسری‌ان** (`PeakDay`) نه per-residence — دیتای قدیم اودو گذشته‌ست پس migrate نشد.
9. **یک درخت مکان به‌جای City/Province.** اودو هم همین بود (`product_public_category` با `parent_id`)؛ دو جدول جدا نمی‌تونست کشور/منطقه/روستا/محله رو نگه داره و به همین خاطر ۳ ست سئو و ۹٬۹۶۹ صفحه‌ی tag_url اصلاً قابل وصل‌شدن نبودن.
10. **بردکرامب ≠ شمول نتایج.** `parentId` فقط مسیر نمایشیه؛ فقط `location_includes` (و استان نسبت به شهرهاش) روی لیست اقامتگاه‌ها اثر می‌ذاره. اگه درخت خودش شمول‌آور بود، سمنان بی‌صدا از ۸ به ۸۳ می‌رفت.
11. **تگ خراب، غیرفعال وارد می‌شه — نه با فیلتر ضعیف‌تر.** اگه شرطی به ویژگی حذف‌شده اشاره کنه، تگ پارک می‌شه؛ وگرنه صفحه‌ی «اجاره باغ ویلا» همه‌ی اقامتگاه‌ها رو لیست می‌کرد.
12. **اسلاگ می‌تونه به چند مکان بخوره و باید اجتماعشون گرفته بشه** — رفتار کد قدیم همین بود و ۱۱ صفحه‌ی پربازدید بهش وابسته‌ن (به «دو تله‌ی اسلاگ» توی بخش سئو نگاه کن).

---

## ⚠️ سه باگ محیطی که وقت زیادی گرفتن (تکرارشون نکن)

1. **`toLocaleString()` بدون locale = کرش hydration.** سرور dev روی ویندوز فارسی رقم فارسی SSR می‌کرد، کلاینت لاتین → ری‌اکت کل SSR رو دور می‌ریخت. همه‌ی ۸۷ مورد به `toLocaleString("en-US")` پین شدن. **قانون: هیچ فرمت locale-وابسته‌ای توی مسیر SSR ننویس.**
2. **استوریج لیارا هر User-Agent شامل «Mozilla» رو ۴۰۴ می‌کنه** (حفاظت بات). یعنی مرورگر هیچ‌وقت مستقیم عکس نمی‌گیره ولی curl/سرور می‌گیرن — دیباگش گمراه‌کننده‌ست. **همه‌ی عکس‌های استوریج باید از `next/image` optimizer رد بشن**؛ توی پنل از `adminImageUrl()` استفاده کن.
3. **hydration توی dev پشت `requestAnimationFrame` گیت می‌شه** — تب پس‌زمینه تا فورگراند نشه hydrate نمی‌شه (بی‌صدا). shim موقت dev-only توی `_document.tsx` هست.

### نکته‌ی Prisma generate روی ویندوز

اگه schema عوض شد و سروری در حال اجراست، `npx prisma generate` با `EPERM` fail می‌شه. راه‌حل: generate توی یه پوشه‌ی موقت (مسیر **ویندوزی**، نه `/tmp`) و بعد کپی فقط `index.js/index.d.ts/schema.prisma/default.js/default.d.ts` — باینری `.dll.node` رو دست نزن.

همچنین `prisma migrate dev` توی این محیط non-interactive کار نمی‌کنه؛ فایل migration رو دستی بساز و `npx prisma migrate deploy` بزن.

---

## 🔍 سیستم سئو (کامل)

### درخت مکان‌ها (`locations`) — جایگزین City/Province

جدول‌های `cities` و `provinces` توی نشست ششم توی **یک جدول درخت‌واره** ادغام شدن:
`COUNTRY › PROVINCE › CITY › REGION › VILLAGE › NEIGHBORHOOD` (۵۶۵ ردیف).

| چی بود | چی شد |
|---|---|
| فقط شهر و استان migrate شده بود | ۵ کشور، ۹ منطقه، ۳ روستا و ۱ محله‌ی اودو هم اومدن |
| `/search/shomal` یه alias هاردکد بود | داده‌ست: `location_includes` |
| «شهرهای زیرمجموعه» اصلاً وارد نشده بود | ۱۹ ردیف `location_includes` |
| فقط ۱ ست سئو (از ۳ ست اودو) | هر ۳ ست توی `location_seo` (۱٬۶۷۵ ردیف): پیش‌فرض / بوم‌گردی / هتل |
| مکان‌ها با نام match می‌شدن | `Location.odooId` ذخیره شده — همه‌چیز روی همون کلید می‌خوره |

**دو رابطه که شبیه‌ان ولی یکی نیستن — قاطی‌شون نکن:**
- `parentId` → فقط **بردکرامب**. لیست اقامتگاه‌ها رو عوض نمی‌کنه.
- `location_includes` → **همون چیزیه که اقامتگاه‌های یه مکان دیگه رو هم نشون می‌ده.**

استان استثناست: صفحه‌ی استان همیشه شهرهاش رو در بر می‌گیره (مثل قبل).
شهر بچه‌هاش رو جذب **نمی‌کنه** — اگه می‌کرد، سمنان از ۸ به ۸۳ اقامتگاه می‌پرید و
صفحه‌های پربازدید بی‌صدا عوض می‌شدن.

### تگ‌های سئو (`seo_tags` + `seo_tag_conditions`)

تعریف تگ‌ها قبلاً **توی ۵ جدول هاردکد و ناسازگار** توی دو سرویس پخش بود
(`TAG_TITLES`، `SUGGESTED_TAGS`، `TAG_AMENITY_FILTERS`، `BINARY_AMENITY_KEYS`/`RULE_KEYS`
و یه کپی پنجم به اسم `RELATED_TAG_RULES` توی سرویس اقامتگاه‌ها). با اودو هم **فرق داشتن**:

| تگ | تعریف اودو | کد قبلی |
|---|---|---|
| `pool` | استخر **و** خانه ویلایی (۱٬۱۰۰ اقامتگاه) | فقط استخر (۱٬۲۶۱) |
| `village` | خانه روستایی **و** (روستایی **یا** حومه شهر) | فقط روستایی |

الان از `website_tags.x_domain` اودو پارس می‌شن (نُتیشن prefix، داخل گروه «یا»، بین گروه‌ها «و»).

**سه تگ عمداً غیرفعال وارد شدن** — `village`، `garden`، `hostel`. دامنه‌شون توی اودو به
attribute value‌هایی اشاره می‌کرد که بعداً حذف شدن (۱۱۱ خانه روستایی، ۲۹۵ باغ، ۱۱۷ هاستل)،
پس **توی خود اودو هم نتیجه‌ای نمی‌دادن**. اگه به «بدون فیلتر» تنزل می‌دادیمشون، صفحه‌ی
«اجاره باغ ویلا» همه‌ی اقامتگاه‌ها رو لیست می‌کرد.

### صفحات تگ×مکان (`tag_pages`)

از `tag_url` اودو، ۹٬۳۱۲ صفحه — شامل **۹٬۳۱۰ متا تایتل دست‌نویس** و **۲۲ متن بدنه** که
مهاجرت قبلی دور ریخته بود و با تمپلیت جایگزین کرده بود. حالا متن دست‌نویس اولویت داره و
تمپلیت فقط fallbackه. ۵۸ صفحه فلگ sitemap دارن. ۱۷ تای‌شون **«تگ مادر»**ن (بدون مکان،
یعنی `/search?pool=1`).

`x_canonical` **عمداً وارد نشد** — به آدرس‌هایی اشاره می‌کرد که خودشون ۳۰۱ می‌خورن
(redirect hop). canonical تازه و مستقیم ساخته می‌شه.

### جدول مسیرها

| URL | رفتار |
|---|---|
| `/search/<slug>` | اسلاگ‌های دست‌ساز تیم از اودو (`x_title_en`) — روی `Location.titleEn`/`name` مچ می‌شه |
| `/tags/<tag>/<فارسی>` | **301** به `/search/<city>?<tag>=1` — ۹٬۸۹۰ مسیر از `legacy_redirects` (دست‌نخورده) |
| `/search/city/<فارسی>-<id>` | **301** به `/search/<slug>` |
| `/web/image/...` | **301** به استوریج — ۱۰۷٬۷۴۳ مسیر (پارک‌شده تا لانچ) |
| `/search/shomal` | حالا از `location_includes` میاد، نه از ثابت هاردکد |

- صفحات تگ×شهر **هویت سئویی مستقل** دارن (H1/متا/canonical مطابق پروداکشن)
- صفحه‌ی میزبان: `<نام> | میزبان لیدوما در <شهر> | لیدوماتریپ` + ProfilePage/Person JSON-LD
- `GET /api/search/page-data?slug=&tags=` منبع همه‌ی متادیتای صفحات سرچه (هم کلاینت هم SSR از همین می‌خونن — **باید هم‌شکل بمونن**)

### ⚠️ دو تله‌ی اسلاگ که حتماً باید بدونی

۱. **یه اسلاگ می‌تونه به چند مکان بخوره.** توی اودو `isfahan` هم شهر اصفهانه هم استان
اصفهان؛ و ردیف‌های تکراری/غلط‌املایی هم هست (`dorud` = درود و دورود، `firuzabad` با
ی عربی و فارسی). کد قدیمی شهر و استان رو با OR می‌گرفت، پس `/search/isfahan` **اجتماع**
هر دو رو نشون می‌داد (۶۵۲ اقامتگاه). اگه فقط بهترین match رو برگردونی، ۱۱ صفحه‌ی
پربازدید بی‌صدا کوچیک می‌شن (isfahan ۶۵۲→۲۴۳، ardabil ۲۲۵→۵۷). برای همین
`resolveLocationsBySlug` **همه** رو برمی‌گردونه و `expandSlugToLocationIds` اجتماعشون
رو می‌گیره؛ `resolveLocationBySlug` (مفرد) فقط برای هویت سئویی صفحه‌ست.

۲. **اسلاگ `s` رزرو شده‌ست.** ریشه‌ی دسته‌بندی اودو («اقامتگاه ها») اسلاگش `s`ه و فرانت
هم برای «شهری انتخاب نشده» همین `s` رو می‌فرسته. اگه به‌عنوان مکان ساخته بشه، متا و
canonical هر `/search` بدون شهر رو می‌دزده. توی `ODOO_ROOT_ID` skip می‌شه.

### دو گیت که نباید بشکنن

```bash
# مدل SQL از معنای مورد انتظار
npx tsx --env-file=.env scripts/verify-location-slugs.ts --compare <snapshot.json>
# کد واقعاً اجراشده: هر ۵۳۵ اسلاگ از خود searchResidences/getSearchPageData رد می‌شه
npx tsx --env-file=.env scripts/verify-search-parity.ts <snapshot.json>
```

هر دو **PASS** هستن، با یک اختلاف عمدی و allowlist‌شده:
`qazvin` ۴۸ → ۷۵، چون `location_includes` قزوین → الموت (۲۷ اقامتگاه) حالا اعمال می‌شه.
اسنپ‌شات قبل از مهاجرت گرفته شده بود، وقتی این فیچر اصلاً وارد نشده بود.

## ✅ چک‌لیست لانچ

1. **canonical مطلق شد** ✓ (انجام شده)
2. **DNS `cdn.lidomatrip.com`** باید به اپ جدید route بشه تا ریدایرکت عکس‌ها کار کنه
3. **باکت لیارا**: حفاظت UA رو بررسی کن — اگه برداشته بشه، بار optimizer کم می‌شه
4. **sitemap**: صفحات میزبان و تگ×شهر باید توش باشن
5. سرور پروداکشن locale انگلیسی داشته باشه (یا مطمئن باشیم همه‌ی فرمت‌ها پین شدن)

---

## چی هنوز مونده

### پنل ادمین (کار فعلی)
1. **تنظیمات** — کاربر گفت خودش طرح می‌ده. اینا توی اودو هست و هنوز نساختیم: تعطیلات رسمی (`x_holidays` ۱۸)، وضعیت همکاری میزبان (`x_host_status` ۲۹ حالت)، دسته/زیردسته‌ی امکانات (`x_attribute_category` ۱۱+۵)، متن راهنمای مراحل ثبت (`x_submit_residence_desc` ۱۳)
2. پنج تب باقی‌مانده‌ی اقامتگاه: رزروها، تقویم، آمار، نظرات، مدرک مالکیت
3. صفحه‌ی مدیریت تصاویر اقامتگاه (`/admin/residences/[id]/images` — لینکش هست، صفحه‌اش نه)
4. بازنویسی صفحه‌ی رزروها با استایل جدید

### بقیه
5. صفحه‌ی اصلی هنوز stub (`emptyHomePageData`)
6. سرویس پیامک واقعی — کلید API نداریم
7. کیف پول، تسویه میزبان، چت رزرو، شکایات، ووچر — فاز ۲
8. درگاه پرداخت — بررسی‌نشده
9. دیپلوی روی سرور واقعی

---

## دقیقاً کجا متوقف شدیم

**نشست ششم (درخت مکان‌ها + سیستم تگ سئو)** — کل تاکسونومی محصولات از اودو اومد و
داده‌محور شد:

- `cities`/`provinces` → یک جدول `locations` (۵۶۵ ردیف، ۶ سطح)
- `location_seo` (۱٬۶۷۵)، `location_includes` (۱۹)، `seo_tags` (۱۸) + `seo_tag_conditions` (۲۲)، `tag_pages` (۹٬۳۱۲)
- سرویس سرچ و سرویس اقامتگاه از روی همین داده‌ها کار می‌کنن — پنج جدول هاردکد حذف شدن
- API ادمین + دو صفحه‌ی پنل: `/admin/settings/locations` و `/admin/settings/tags`
- هر دو گیت parity سبزن (با یک اختلاف عمدی: qazvin ۴۸→۷۵)

**تأییدشده روی مرورگر (نشست ششم):**

| صفحه | نتیجه |
|---|---|
| `/search/shiraz` | ۳۴۶ آگهی · H1 و canonical درست |
| `/search/shiraz?pool=1` | متای **دست‌نویس** اودو (نه تمپلیت) · ۶ آگهی · canonical با `?pool=1` |
| `/search/shomal` | ۳٬۰۳۱ آگهی از مازندران و گیلان — `location_includes` جای alias هاردکد |
| `/search/qazvin` | ۷۵ آگهی از «قزوین» و «الموت» — فیچر شهر زیرمجموعه |
| `/search?pool=1` | تگ مادر · ۱٬۱۰۰ آگهی (تعریف واقعی اودو) |
| `/search/shiraz?garden=1` | تگ غیرفعال: بدون برند جعلی، canonical به صفحه‌ی ساده‌ی شهر |

**قدم بعدی:**

1. **sitemap** باید `tag_pages` (۵۸ ردیف با فلگ) و صفحات مکان رو در بر بگیره.
3. سه تگ غیرفعال (`village`، `garden`، `hostel`) منتظر تصمیم کاربرن — یا شرط درست
   براشون تعریف بشه یا همین‌طور پارک بمونن.
4. تصمیم باز: آیا `ResidenceLocation` («دیگر شهرهای اقامتگاه»، ۸۵۹ ردیف) باید توی
   نتایج سرچ هم اعمال بشه؟ الان **نمی‌شه** — نه قبلاً می‌شد نه الان. اعمالش تعداد
   نتایج بعضی شهرها رو زیاد می‌کنه، پس عمداً دست‌نخورده مونده تا کاربر تصمیم بگیره.
5. پنج تب باقی‌مانده‌ی اقامتگاه: رزروها، تقویم، آمار، نظرات، مدرک مالکیت.
6. صفحه‌ی مدیریت تصاویر اقامتگاه (`/admin/residences/[id]/images`).
