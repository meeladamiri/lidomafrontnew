# وضعیت پروژه — مهاجرت لیدوماتریپ از Odoo به بک‌اند جدید

آخرین به‌روزرسانی: 2026-08-23

> این فایل «نقشه‌ی برگشت» پروژه‌ست. اگه یه چت جدید (یا ادامه‌ی همین چت بعد از خلاصه‌سازی خودکار) این پروژه رو باز کرد، خوندن همین یه فایل باید کافی باشه بفهمه دقیقاً کجای کاریم. کد واقعی همیشه توی گیت‌هابه، نه توی تاریخچه‌ی چت — پس اگه مکالمه از دست بره چیزی از دست نمی‌ره.
>
> این فایل همیشه **آخرین وضعیت** رو نشون می‌ده (هر بار بازنویسی می‌شه). یه کپی تاریخ‌دار از هر نسخه هم توی `docs/progress/YYYY-MM-DD_HH-MM.md` آرشیو می‌شه.

---

## این پروژه دقیقاً چیه؟

**لیدوماتریپ قبلاً روی Odoo اجرا می‌شد** — بک‌اند production واقعی (`lidomatrip.com`) هنوز روی Odoo زنده‌ست. هدف: مهاجرت کامل به یه بک‌اند جدید و مستقل (Express + TypeScript + Prisma + PostgreSQL)، بدون دست‌زدن به کامپوننت‌های UI فرانت‌اند قدیمی (فقط لایه‌ی `api/*.ts` عوض می‌شه).

**مرحله‌ی «کارکردی»** (فاز ۱) از قبل تموم شده بود. **مرحله‌ی «دیتای واقعی»** (بزرگ‌ترین آیتم باقی‌مونده) هم امروز عملاً کامل شد — جزئیات کامل پایین‌تر.

---

## 🟢 وضعیت مهاجرت دیتای واقعی — عملاً کامل

| بخش | وضعیت | تعداد نهایی |
|---|---|---|
| کاربران (res_partner) | ✅ کامل | ۲۸۶,۵۲۷ (شامل حساب بانکی و پرچم میزبان) |
| عکس اقامتگاه‌ها (اصلی+گالری+اتاق) | ✅ کامل | ۱۴۷,۵۱۱ فایل روی Liara Object Storage |
| اقامتگاه‌ها + اتاق‌ها | ✅ کامل | ۹,۵۷۰ اقامتگاه، ۱۸,۴۴۹ اتاق |
| مختصات جغرافیایی (lat/lng) | ✅ کامل (backfill) | ۸,۵۲۱ اقامتگاه |
| استان‌ها و شهرها | ✅ کامل | ۲۷ استان، ۵۲۱ شهر |
| `cityId` روی اقامتگاه‌ها | ✅ کامل (backfill) | ۷,۳۲۰ اقامتگاه |
| اسلاگ انگلیسی شهر (`titleEn`) | ✅ کامل (transliteration) | ۵۱۷ شهر |
| رزروها (فقط `state=sale` واقعی) | ✅ کامل | ۲۹,۶۴۳ رزرو (همه `DONE`) |
| آواتار میزبان (فقط عکس‌های واقعی، نه پیش‌فرض) | ✅ کامل | ۱,۴۵۳ میزبان |
| تقویم/قیمت روزانه (فقط آینده) | ✅ کامل | ۵,۲۵۸ روز تقویمی |
| کاتالوگ امکانات/قوانین (x_rooms_features) | ⬜ هنوز نه | نیاز به طراحی taxonomy تمیز |

### دستورهای اجراشده (به ترتیب، هرکدوم idempotent، توی `backend/`)

```bash
npx tsx scripts/migrate-odoo-users.ts --commit
npx tsx scripts/migrate-odoo-residences.ts --commit   # قبلش scripts/upload-images.js باید رو سرور مبدأ اجرا بشه، بعد image-map.json دانلود بشه
npx tsx scripts/migrate-odoo-cities.ts --commit         # استان/شهر + backfill cityId
npx tsx scripts/migrate-odoo-reservations.ts --commit
npx tsx scripts/backfill-odoo-latlng.ts --commit
npx tsx scripts/backfill-city-slugs.ts --commit
npx tsx scripts/migrate-odoo-calendar.ts --commit
# روی سرور مبدأ (SSH):
node --experimental-global-webcrypto upload-host-avatars.js   # بعدش host-avatar-map.json دانلود بشه
npx tsx scripts/backfill-host-avatars.ts --commit
```

**نکته‌ی تکرارپذیری مهم:** همه‌ی این اسکریپت‌ها idempotent هستن (چک existence قبل از نوشتن) — اگه هرکدوم وسط راه با خطای connection موقت (خیلی زیاد پیش اومد، چون Liara زیر بار migration سنگین کند می‌شه) fail بشه، فقط دوباره همون دستور رو بزن، از همونجا ادامه می‌ده.

### چیزی که هنوز باز مونده در مهاجرت دیتا

فقط **کاتالوگ امکانات/قوانین** (از `x_rooms_features` Odoo) باقی مونده — چون کاتالوگ هدف (`amenities`/`rules`) هنوز تقریباً خالیه و نیاز به طراحی یه taxonomy تمیز داره، نه صرفاً کپی مکانیکی.

---

## جزئیات فنی مهاجرت (منابع و روش کار)

### دسترسی به سرور مبدأ

- **SSH:** `164.138.22.18:1122`, user `root` (پسورد در تاریخچه‌ی چت).
- بکاپ‌های دیتابیس: `/backupnew/latest-backup.dump`. filestore عکس‌ها: `/media/filestore/liduma/`.
- ابزارها: `node` v16.19.1 (برای AWS SDK v3 باید با `--experimental-global-webcrypto` اجرا بشه)، `@aws-sdk/client-s3` (نصب‌شده توی `/root/migrate/`).

### دیتابیس مقصد

- Liara Postgres همون که بک‌اند استفاده می‌کنه، پلن **«زحل»** (۴ گیگ رم، ۴۰-۵۰ گیگ دیسک).
- `DATABASE_URL` با `connection_limit=20`.
- کل دامپ Odoo توی یه دیتابیس جدا به اسم **`odoo_legacy`** روی همون سرور Liara restore شده (۱۵ گیگ، ۵۵۵ جدول) — منبع همه‌ی کوئری‌های migration.

### یافته‌های کلیدی درباره‌ی منابع داده‌ی Odoo (برای مهاجرت‌های بعدی مفیده)

| چیزی که دنبالش بودیم | جدول واقعی Odoo | نکته |
|---|---|---|
| کاربران | `res_partner` | ۳۱۳,۴۵۳ ردیف؛ دیدوپ بر اساس شماره تلفن نرمال‌شده |
| اقامتگاه‌ها | `product_template` (`website_published=true`) | ۱۴,۱۸۵ منتشرشده |
| اتاق‌ها | `x_room` | لینک از طریق `x_template_id` |
| عکس‌ها | `ir_attachment` (`res_model='product.template'/'product.image'`) | باینری‌ها **در filestore**، نه دیتابیس |
| شهر/استان **تمیز** | `product_public_category` (`x_category_type IN ('city','province')`) | نه فیلد متنی به‌هم‌ریخته‌ی `x_city`! لینک اقامتگاه از طریق `x_main_category` |
| رزروها | `sale_order` (`state='sale'`) | فقط ۴۳,۹۴۷ از ۳۴۸,۰۷۲ ردیف واقعاً معتبرن؛ بقیه لغوشده/سبد رهاشده |
| تقویم/قیمت روزانه | `x_special_dates` + `x_fast_dates` | **نه `x_hotel_date_price` که خالی بود!** فقط ۶٪ ردیف‌ها تاریخ آینده دارن |
| آواتار میزبان | `ir_attachment` (`res_model='res.partner'`) | اکثرش آواتار پیش‌فرض خودکار Odoo‌ست (فایل تکراری بین ۵۰۰+ نفر)، فقط repeat_count≤۱۰ عکس واقعیه |

### تصمیمات کلیدی مهاجرت

1. **رمز عبور منتقل نمی‌شه** (هش Odoo با bcrypt جدید ناسازگاره) — کاربرهای قدیمی با OTP لاگین می‌کنن.
2. **دیدوپ کاربران بر اساس شماره‌تلفن نرمال‌شده**؛ رکورد دارای `res_users` یا `write_date` جدیدتر برنده می‌شه.
3. **hostId/guestId/residenceId همیشه از طریق resolve کردن phone→user یا `reference="ODOO-<id>"`→residence پیدا می‌شن** — یعنی ترتیب اجرا مهمه: کاربران قبل از اقامتگاه‌ها، اقامتگاه‌ها قبل از رزروها/تقویم.
4. **فقط دیتای واقعی و فعلاً مرتبط migrate می‌شه**، نه هرچی هست: فقط اقامتگاه‌های منتشرشده، فقط رزروهای واقعاً تأییدشده (`state=sale`)، فقط تاریخ‌های تقویمِ آینده، فقط آواتارهای واقعی (نه پیش‌فرض). این‌ها همه تصمیم آگاهانه بودن برای جلوگیری از شلوغی بی‌فایده، نه محدودیت فنی.
5. **عکس‌ها فقط URL (نه باینری) توی دیتابیس** — آپلود مستقیم از سرور مبدأ به باکت Liara (`lidoma-uploads`) با کلید `migrated/...`.
6. اسکریپت‌ها همیشه dry-run پیش‌فرض (`--commit` برای نوشتن واقعی) و idempotent.
7. **Concurrency باید با `connection_limit` هماهنگ باشه** (درس گرفته‌شده از یه تجربه‌ی بد اولیه با pool timeout).
8. **موقع نوشتن exists-check با یه فیلد nullable توی compound unique key** (مثل `CalendarDay`'s `roomId`)، از `findFirst` استفاده کن نه `findUnique` — Prisma با `null` توی `findUnique` روی compound key خطا می‌ده حتی اگه ستون nullable باشه.
9. **PostgreSQL auto-increment sequence با seed data دستی هماهنگ نیست به‌صورت پیش‌فرض** — اگه یه جدول از قبل چندتا ردیف seed با id دستی داشته باشه، اولین `create()` بعدی ممکنه به unique constraint بخوره. رفعش: `SELECT setval('<table>_id_seq', (SELECT MAX(id) FROM <table>));`

### ⚠️ نکته‌ی Prisma generate روی Windows (وقتی سرور dev یا اسکریپتی در حال اجراست)

اگه schema.prisma عوض بشه ولی یه پردازش (سرور dev یا اسکریپت migration) در حال اجرا باشه، `npx prisma generate` با `EPERM: operation not permitted, unlink ...query_engine-windows.dll.node` fail می‌شه. راه‌حل بدون کشتن پردازش زنده:

```bash
sed 's#output   = "../src/generated/prisma/client"#output   = "../src/generated/prisma-tmp/client"#' prisma/schema.prisma > prisma/schema-tmp.prisma
npx prisma generate --schema=./prisma/schema-tmp.prisma
cp src/generated/prisma-tmp/client/{index.js,index.d.ts,schema.prisma,default.js,default.d.ts} src/generated/prisma/client/
rm -rf src/generated/prisma-tmp prisma/schema-tmp.prisma
```
(باینری `.dll.node` عمداً کپی نمی‌شه — schema-specific نیست، نیازی به تعویض نداره.)

---

## دو پنل، دو مبدأ متفاوت

| | پنل مشتری/میزبان (`front`) | پنل کارمندان/ادمین (`front/admin`) |
|---|---|---|
| **استایل** | Tailwind کامل | CSS دستی (`admin.css`) برای صفحات قدیمی؛ **صفحات جدید از این به بعد Tailwind** (از قبل روی `pages/admin/**` فعاله) |
| **مسیر** | `front` | همون پوشه، `front/pages/admin`, `front/components/Admin` |

## ساختار پروژه

```
Desktop/isnad/lidomatrip/lidomatripnew/
  ├── backend/   → https://github.com/meeladamiri/lidomabacknew   (branch: master)
  └── front/     → https://github.com/meeladamiri/lidomafrontnew  (branch: main)
```

Liara Postgres (`cho-oyu.liara.cloud:33283`)، Liara Object Storage (`storage.iran.liara.site`, bucket `lidoma-uploads`). هیچ‌کدوم پروداکشن دیپلوی نشده — فقط dev محلی.

### دسترسی تست

- میزبان تستی: `09121111111` / `Host123456!` — ادمین تستی: `09120000000` / `ChangeMe123!`
- OTP فقط توی لاگ ترمینال بک‌اند چاپ می‌شه.

---

## چه چیزهایی کامل ساخته و تست شده

### از قبل (فاز ۱ اصلی)
Auth، جستجو، مشاهده‌ی اقامتگاه، رزرو، ویزارد ثبت اقامتگاه (۱۵ مرحله)، تقویم/قیمت‌گذاری میزبان، آپلود فایل.

### نشست‌های اخیر (به‌ترتیب)

- **پروفایل کاربر** (آپلود آواتار + ویرایش اطلاعات) وصل به بک‌اند جدید.
- **پنل ادمین:** جزئیات کاربر (+ رفع نشتی امنیتی `passwordHash`)، جزئیات رزرو (نمایش کامل + ۳ اکشن: `forceApprove`/`markDone`/`cancel`)، جزئیات اقامتگاه (۶ تب کاملاً قابل‌ویرایش، با استفاده‌ی مجدد از سرویس‌های سمت میزبان)، لیست اقامتگاه‌ها (toggle کارتی/ردیفی + فیلتر سفارشی + پریست ذخیره‌شده در دیتابیس).
- **پروفایل عمومی میزبان** (`GET /residences/hosts/:hostId`) — UI از قبل آماده بود، فقط endpoint کم داشت.
- **سیستم نظرات کامل از صفر** — مدل `Review` (۶ زیرامتیاز + میانگین + پاسخ میزبان)، endpoint های مهمان/میزبان، `averageRating`/`reviewsCount` که قبلاً همیشه صفر بودن الان واقعی محاسبه می‌شن.
- **مهاجرت کامل دیتای واقعی** (بالا توضیح داده شد).
- **رفع ۸ باگ واقعی پیدا‌شده با تست دستی مرورگر** روی صفحات اقامتگاه‌ی migrate‌شده:
  1. بردکرامب آیتم چهارم (اسم اقامتگاه) نداشت — `Infography/index.tsx` از فیلد خالی `name2` به‌جای اسم اصلی استفاده می‌کرد.
  2. `SUIT`/`BOOMGARDI` خام (بدون ترجمه) توی چند جای صفحه نمایش داده می‌شد — `observe.ts` باید از `mapResidenceTypeLabel` جدید استفاده کنه.
  3. `extra_price` همیشه صفر بود (اسم فیلد اشتباه: `extraPrice` به‌جای `extraGuestsPrice`).
  4. **بعد از ثبت رزرو صفحه `/my-trips/undefined` می‌شد** — بک‌اند `{reservation, pricing}` برمی‌گردوند، فرانت دنبال `data.id` (نه `data.reservation.id`) می‌گشت.
  5. پیام خطای رزرو نمایش داده نمی‌شد (`err_msg` به‌جای `message`).
  6. ریسک کرش کامپوننت آواتار میزبان بدون fallback.
  7. عکس‌ها/آواتار/اقامتگاه‌های مشابه نمایش داده نمی‌شدن — **نه باگ کد، دامنه‌ی Liara به `next.config.js` اضافه شده بود ولی سرور dev ری‌استارت نشده بود** (Next.js این تغییر رو hot-reload نمی‌کنه).
  8. سه‌گانه‌ی «دیتای مهاجرت‌نشده» (آواتار میزبان، تقویم، اسلاگ شهر) که در ادامه‌ی همین نشست کامل شدن.

**محدودیت محیطی ثابت:** تست‌ها همیشه از راه API مستقیم (curl با توکن واقعی) یا SSR-fetch بوده، نه کلیک واقعی مرورگر — این محیط قادر به اجرای قابل‌اعتماد تعامل مرورگر نیست. **کاربر پروژه خودش با کلیک واقعی توی مرورگر تست می‌کنه و باگ گزارش می‌ده** — همین باعث پیدا شدن ۸ باگ بالا شد.

---

## تصمیمات معماری کلیدی (باید رعایت بشه)

1. **Reshape در لایه‌ی API، نه دست‌کاری کامپوننت‌ها.** وقتی تایپ دقیق‌تر باعث خطای تایپ توی کامپوننت قدیمی می‌شه، فقط type cast تک‌خطی اضافه کن.
2. **فیلدهای غایب همیشه default امن** (`[]`، نه `undefined`).
3. **قابلیت بدون معادل بک‌اند:** `Promise<any>` که `{status:"error", err_msg:"..."}` برمی‌گردونه.
4. **بعد از هر تغییر: `npx tsc --noEmit` توی هر دو پروژه صفر خطا.**
5. **قبل از commit به `front`:** `yarn.lock` رو با `npx yarn@1.22.19 add ...` دست بزن، نه `npm install`.
6. **endpoint های ادمین معادل سمت میزبان/مهمان:** به‌جای کپی منطق، hostId/ownership واقعی رو resolve کن و همون سرویس موجود رو صدا بزن.
7. **صفحات جدید پنل ادمین: Tailwind.** قدیمی‌ها تدریجی بازنویسی می‌شن.
8. **next.config.js عوض شد؟ سرور dev رو کامل ری‌استارت کن** — Next.js تغییرات این فایل رو hot-reload نمی‌کنه.

---

## چی هنوز مونده (اولویت‌بندی‌شده)

### فاز ۱ — باقیمانده

1. **کاتالوگ امکانات/قوانین واقعی** (از `x_rooms_features`) + وصل‌کردن فیلتر جستجو (`front/api/Search/search.ts`'s `buildSearchBody` فیلد `features` رو اصلاً به بک‌اند نمی‌فرسته — یه گپ جداگانه‌ی قبل‌ازاین‌موجود، نه مربوط به migration).
2. صفحه‌ی اصلی هنوز کاملاً stub (بخش‌های CMS مثل بنر/پیشنهادها) — `pages/index.tsx` با `emptyHomePageData` هاردکد شده.
3. کارت‌های دسته‌بندی صفحه‌ی اصلی (ویلا/سوئیت/بوم‌گردی/...) به مسیرهایی لینک می‌شن که اصلاً وجود ندارن (404) — پیش‌ازاین موجود، نه مربوط به migration.
4. سرویس پیامکی واقعی — کلید API نداریم.
5. نقش «کارشناس» جدا از ادمین کامل.
6. بوم‌گردی سطح-اتاق (رزرو/قیمت‌گذاری per-room).
7. کیف پول، تسویه میزبان، چت رزرو، شکایات، ووچر — Phase 2 بک‌اند.
8. درگاه پرداخت — بررسی‌نشده.
9. دیپلوی روی سرور واقعی.
10. بازنویسی تدریجی صفحات قدیمی پنل ادمین با Tailwind.

### فاز ۲ — سئوی فرانت‌اند مهمان

11. بازبینی سئوی صفحه‌ی اصلی/جستجو/اقامتگاه (متادیتا، structured data، سرعت).
12. بررسی صفحات هنوز CSR که باید SSR بشن.

---

## دقیقاً کجا متوقف شدیم

مهاجرت کامل دیتای واقعی (کاربران، عکس‌ها، اقامتگاه‌ها، اتاق‌ها، شهر/استان، رزروها، مختصات، آواتار میزبان، تقویم) تموم شد. کاربر با تست دستی مرورگر ۸ باگ واقعی روی صفحات اقامتگاه پیدا کرد که همه رفع و تأیید شدن (breadcrumb، ترجمه‌ی نوع اقامتگاه، فیلد قیمت اضافه، باگ اصلی «بعد از رزرو undefined می‌شه»، پیام خطا، گارد آواتار، و مشکل عدم نمایش عکس که با ری‌استارت سرور حل شد).

**قدم بعدی طبیعی:** کاتالوگ امکانات/قوانین (آیتم ۱ لیست بالا) — تنها آیتم مهاجرت دیتا که باز مونده، ولی چون کاتالوگ هدف خالیه، اول نیاز به طراحی taxonomy داره، نه صرفاً یه اسکریپت دیگه. یا اگه کاربر ترجیح بده، می‌شه سراغ فیکس‌های پیش‌ازاین‌موجود (فیلتر تگ/امکانات که کار نمی‌کنه، لینک‌های ۴۰۴ صفحه‌ی اصلی) رفت.
