export interface IDiscountAmounts {
  special_days_total_discount_amount: number;
  peak_days_total_discount_amount: number;
  weekends_total_discount_amount: number;
  normaldays_total_discount_amount: number;
}
