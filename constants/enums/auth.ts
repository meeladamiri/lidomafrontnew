export const enum Auth {
  IS_SIGN_UP = "is_sign_up", // if true, user should register.
  MIZBAN_PHONE_NUMBER = "MIZBAN_PHONE_NUMBER",
  HOST_ID = "HOST_ID",
  IS_FROM_FORGET_PASSWORD = "is_from_forget_password",
  OTP_CODE = "otp_code",
}
