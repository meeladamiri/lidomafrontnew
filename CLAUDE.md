# لیدوماتریپ — فرانت

Next.js 13 (pages dir). بک‌اند در مخزن جداگانه‌ی `../backend` است.

**قبل از هر کاری `docs/PROGRESS.md` را بخوان** — منبع حقیقت پروژه است و می‌گوید
دقیقاً کجای کاریم. بعدش:

| فایل | چه وقت |
|---|---|
| [`docs/OPEN-BUGS.md`](docs/OPEN-BUGS.md) | قبل از شروع هر کار تازه — شاید همان است |
| [`docs/ADMIN-PITFALLS.md`](docs/ADMIN-PITFALLS.md) | قبل از دست‌زدن به پنل ادمین |
| [`docs/DEPLOY.md`](docs/DEPLOY.md) | قبل از دیپلوی |

جدول کامل مستندات در `PROGRESS.md` است — برای هر حوزه (تقویم، چت، کیف پول،
اطلاعیه، جاذبه‌ها، …) یک فایل جدا هست. همان موقع بخوانش، نه بعد از نوشتن کد.

---

## قواعدی که نقض‌شدنشان یک بار به ما ضربه زده

**تا وقتی `next dev` بالاست هرگز `npm run build` نزن.** هر دو روی یک پوشه‌ی
`.next` می‌نویسند و سرور در حال اجرا خراب می‌شود. برای تایپ‌چک `npx tsc --noEmit`.

**بریک‌پوینت‌های Tailwind این پروژه استاندارد نیستند.** `sm:` یک **بازه‌ی
۷۶۸ تا ۱۰۲۳** است، نه min-width. برای دسکتاپ `md:` (۱۰۲۴+) یا `lg:` (۱۲۱۶+).
کلاسی که با `sm:` نوشته شود روی دسکتاپ **اعمال نمی‌شود**.

**کد اقامتگاه با id داخلی فرق دارد.** `publicId` چیزی است که کاربر می‌بیند؛
۱٬۶۴۰ کد اودو همزمان id داخلیِ اقامتگاه *دیگری* هستند. لینک یا فراخوانی با عدد
اشتباه، بی‌صدا روی اقامتگاه اشتباه می‌افتد. `publicResidenceId()` را به کار ببر.

**دیپلوی: فقط `npm run deploy`، نه `liara deploy` مستقیم.** بیلد لیارا دیگر
tsc و lint نمی‌زند؛ آن بررسی‌ها به `npm run verify` منتقل شده‌اند. مستقیم زدن
یعنی دور زدن تنها شبکه‌ی ایمنی. **اول بک‌اند تا آخر، بعد فرانت.**

**دیپلوی فقط وقتی کاربر بخواهد**، و همه‌ی تغییرات یکجا — نه بعد از هر تغییر.

**`.env` در گیت نیست و نباید برود.**

---

## سبک کار

- گزارش پایان نوبت کوتاه. توکن‌محتاط کار کن؛ فراخوانی اضافه نزن.
- فرانت را کاربر خودش در مرورگر چک می‌کند.
- بعد از هر تغییر `docs/PROGRESS.md`، هر دو مخزن commit و push شوند.
- هنگام پورت‌کردن صفحات قدیمی: URLهای ایندکس‌شده با ۳۰۱ حفظ شوند، ولی اشتباهات
  سئوی قدیمی **تکرار نشوند** — به کاربر گزارش شوند.

## حساب‌های تست

ادمین `09120000000` · میزبان `09120000001` · مهمان `09120000002` — رمز همه `123456`
